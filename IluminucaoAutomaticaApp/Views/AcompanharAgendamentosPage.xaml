<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:componentes="clr-namespace:IluminucaoAutomaticaApp.Views.Componentes"
             xmlns:viewmodels="clr-namespace:IluminucaoAutomaticaApp.ViewModels"
             x:Class="IluminucaoAutomaticaApp.Views.AcompanharAgendamentosPage"
             BackgroundColor="#e7edf8"
             NavigationPage.HasNavigationBar="False">

    <ContentPage.BindingContext>
        <viewmodels:AgendamentoPageViewModel />
    </ContentPage.BindingContext>

    <Grid RowDefinitions="Auto,*">
        <componentes:CabecalhoView Grid.Row="0"/>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="15">
                <Label Text="Acompanhar Agendamentos"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="Black"
                       HorizontalOptions="Center"
                       Margin="0,10"/>

                <Label Text="Agendamentos pendentes:"
                       FontSize="20"
                       FontAttributes="Bold"
                       TextColor="DarkSlateGray"
                       TextDecorations="Underline"
                       HorizontalOptions="Center"/>

                <ActivityIndicator IsRunning="{Binding Carregando}"
                   IsVisible="{Binding Carregando}"
                   Color="DarkGray"
                   HorizontalOptions="Center"
                   Margin="0,10" />

                <Label x:Name="LabelSemAgendamento"
                       Text="A lâmpada não possui nenhum agendamento pendente!"
                       Padding="10"
                       HorizontalTextAlignment="Center"
                       TextColor="DarkRed"
                       FontAttributes="Italic"
                       FontSize="18"
                       HorizontalOptions="Center"
                       Opacity="0"
                       IsVisible="False"/>

                <CollectionView ItemsSource="{Binding Agendamentos}"
                            Margin="30,0"
                            HorizontalOptions="Fill">

                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical"/>
                    </CollectionView.ItemsLayout>

                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="#3a79ff"
                               BorderColor="DarkSlateGray"
                               CornerRadius="20"
                               Padding="15"
                               Margin="0,10">

                                <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                                    <Label Text="{Binding Nome, StringFormat='Nome: {0}'}"
                                       TextColor="White"
                                       FontSize="19"
                                       HorizontalOptions="Center"
                                       FontAttributes="Bold"/>
                                    <Label Text="{Binding MomentoAcionamento, StringFormat='Momento: {0}'}"
                                       TextColor="White"
                                       HorizontalOptions="Center"
                                       FontSize="18"
                                       FontAttributes="Bold"/>
                                    <Label Text="{Binding Acao, StringFormat='Ação: {0}'}"
                                       TextColor="White"
                                       HorizontalOptions="Center"
                                       VerticalOptions="Center"
                                       FontSize="18"
                                       FontAttributes="Bold"
                                       HorizontalTextAlignment="Center"
                                       VerticalTextAlignment="Center"
                                       LineBreakMode="WordWrap"/>
                                </VerticalStackLayout>

                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>